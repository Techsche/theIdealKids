export interface IBanner {
  id: string;
  description: string;
  imagePath: string;
  is_enable: boolean;
  message: string;
  redirectUrl: string;
  sort_order: number;
  success: boolean;
  text: string;
}
